@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Error.</h1>
<h2 class="text-danger">Se produjo un error al procesar su solicitud.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>Modo de desarrollo</h3>
<p>
    Cambie a <strong>Desarrollo</strong> para ver información detallada sobre el error que ocurrió.
</p>
<p>
    <strong>El entorno de desarrollo no debe habilitarse para aplicaciones implementadas.</strong>
    Puede resultar en la visualización de información confidencial de excepciones a los usuarios finales.
    Para la depuración local, habilite el entorno <strong>Desarrollo</strong> configurando la variable de entorno <strong>ASPNETCORE_ENVIRONMENT</strong> en <strong>Development</strong>
    y reiniciando la aplicación.
</p>